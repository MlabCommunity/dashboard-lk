import DashboardWrapper from "shared/dashboard/DashboardWrapper";
import Nav from "shared/dashboard/Nav";
import Panel from "shared/dashboard/Panel";

const MainDashboard = () => (
  <DashboardWrapper>
    <Nav />
    <Panel title="daddadada" />
  </DashboardWrapper>
);

export default MainDashboard;
